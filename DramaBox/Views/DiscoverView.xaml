<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="DramaBox.Views.DiscoverView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Shell.NavBarIsVisible="False"
    NavigationPage.HasNavigationBar="False"
    NavigationPage.HasBackButton="False">

    <Grid Padding="18" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Topo -->
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto" VerticalOptions="Center">
            <Label Text="Descobrir"
                   Style="{StaticResource TitleLargeLabelStyle}" />

            <Button Grid.Column="1"
                    Text="⚙"
                    BackgroundColor="Transparent"
                    WidthRequest="44"
                    HeightRequest="44"
                    CornerRadius="14" />
        </Grid>

        <!-- Busca -->
        <Border Grid.Row="1" Style="{StaticResource InputContainerStyle}">
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10" VerticalOptions="Center">
                <Label Text="🔎" FontSize="16" VerticalTextAlignment="Center" />
                <Entry Placeholder="Buscar séries, gêneros, criadores..."
                       Style="{StaticResource InputEntryStyle}" />
            </Grid>
        </Border>

        <!-- Conteúdo -->
        <ScrollView Grid.Row="2">
            <VerticalStackLayout Spacing="14">

                <!-- Destaque -->
                <Border Style="{StaticResource CardBorderStyle}">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="Em alta hoje"
                               Style="{StaticResource TitleMediumLabelStyle}" />
                        <Label Text="Sugestões e tendências (mock)."
                               Style="{StaticResource MutedLabelStyle}" />

                        <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                            <Border Style="{StaticResource CardBorderStyle}">
                                <VerticalStackLayout Spacing="6">
                                    <Label Text="Top 1" FontAttributes="Bold" />
                                    <Label Text="Drama • 8 eps" Style="{StaticResource MutedLabelStyle}" />
                                </VerticalStackLayout>
                            </Border>

                            <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                                <VerticalStackLayout Spacing="6">
                                    <Label Text="Top 2" FontAttributes="Bold" />
                                    <Label Text="Suspense • 5 eps" Style="{StaticResource MutedLabelStyle}" />
                                </VerticalStackLayout>
                            </Border>
                        </Grid>
                    </VerticalStackLayout>
                </Border>

                <!-- Lista (placeholder) -->
                <Border Style="{StaticResource CardBorderStyle}">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="Para você"
                               Style="{StaticResource TitleMediumLabelStyle}" />
                        <Label Text="Aqui entra o feed com cards e thumbs (mock)."
                               Style="{StaticResource MutedLabelStyle}" />

                        <VerticalStackLayout Spacing="10">
                            <Border Style="{StaticResource CardBorderStyle}">
                                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                    <BoxView WidthRequest="54" HeightRequest="54" CornerRadius="14" Opacity="0.15" />
                                    <VerticalStackLayout Grid.Column="1" Spacing="2">
                                        <Label Text="Série exemplo 1" FontAttributes="Bold" />
                                        <Label Text="Drama • 8 episódios" Style="{StaticResource MutedLabelStyle}" />
                                    </VerticalStackLayout>
                                </Grid>
                            </Border>

                            <Border Style="{StaticResource CardBorderStyle}">
                                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                    <BoxView WidthRequest="54" HeightRequest="54" CornerRadius="14" Opacity="0.15" />
                                    <VerticalStackLayout Grid.Column="1" Spacing="2">
                                        <Label Text="Série exemplo 2" FontAttributes="Bold" />
                                        <Label Text="Suspense • 5 episódios" Style="{StaticResource MutedLabelStyle}" />
                                    </VerticalStackLayout>
                                </Grid>
                            </Border>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>

                <BoxView HeightRequest="8" Opacity="0" />
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
